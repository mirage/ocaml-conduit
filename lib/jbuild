(jbuild_version 1)
(library
 ((name        conduit)
  (public_name conduit)
  (wrapped false)
  (preprocess (pps (ppx_sexp_conv)))
  (modules (conduit conduit_trie resolver))
  (libraries (ipaddr uri astring))))
